version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - cd api-blsheet
      - npm ci

  build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - api-blsheet/**/*
    - scripts/**/*
    - appspec.yml
  exclude-paths:
    - api-blsheet/node_modules/**/*
