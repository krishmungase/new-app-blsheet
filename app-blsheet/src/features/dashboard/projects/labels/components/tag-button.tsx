import { Button } from "@/components";
import { Tag } from "lucide-react";

const TagButton = () => {
  return (
    <Button size="sm" className="ring-0">
      <Tag /> Labels
    </Button>
  );
};

export default TagButton;
